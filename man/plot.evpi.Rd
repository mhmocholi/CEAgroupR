% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.evpi.R
\name{plot.evpi}
\alias{plot.evpi}
\title{Plot Expected Value of Perfect Information (EVPI) Curves}
\usage{
\method{plot}{evpi}(
  data,
  mode = "overall",
  subgroups = NULL,
  lambda_range = c(0, 1e+05),
  steps = 100,
  facet = FALSE,
  facet_by = c("vars", "levels"),
  palette = "Set2"
)
}
\arguments{
\item{data}{A \code{cea_results}, \code{cea_results_list}, or tibble from
\code{\link{combine_icers_results}}.}

\item{mode}{Character. Visualization mode: one of
\code{"overall"}, \code{"subgroups"}, or \code{"datasets_subgroups"}.}

\item{subgroups}{Optional character vector specifying which subgroup
variables to include (for subgroup modes).}

\item{lambda_range}{Numeric vector of length 2 giving the range of lambda
(default = c(0, 100000)).}

\item{steps}{Integer. Number of lambda points (default = 100).}

\item{facet}{Logical. If TRUE, facet the plot appropriately for the mode.}

\item{facet_by}{Character. For subgroup modes, determines faceting scheme:
\code{"vars"} (facet by subgroup variable) or \code{"levels"} (facet by subgroup level).}

\item{palette}{Color palette from RColorBrewer (default = "Set2").}
}
\value{
A ggplot object representing the EVPI curves.
}
\description{
Computes and plots the Expected Value of Perfect Information (EVPI)
as a function of willingness-to-pay (lambda) using bootstrap replicates
from one or several analyses. The structure of modes and faceting
matches that of \code{\link{plot.icers}} and \code{\link{plot.ceac}}.
}
