% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.ceacs.R
\name{plot.ceacs}
\alias{plot.ceacs}
\title{Plot Cost-Effectiveness Acceptability Curves (CEAC)}
\usage{
\method{plot}{ceacs}(
  data,
  color_by = NULL,
  shape_by = NULL,
  facet_by = NULL,
  mode = "Overall",
  filter_expr = NULL,
  facet_scales = "fixed",
  lambda_max = NULL,
  lambda_steps = 100,
  show_probabilities = NULL,
  show_lambdas = FALSE,
  lambdas = NULL,
  palette = NULL,
  return_data = FALSE,
  ...
)
}
\arguments{
\item{data}{A cea_results_list object or a tibble returned by
combine_icers_results().}

\item{color_by}{Character variable mapped to colour, or "none".}

\item{shape_by}{Character variable mapped to shape, or "none".}

\item{facet_by}{Faceting directive ("none", "dataset", "comparison",
"subgroup_var", "subgroup_level", "subgroup").}

\item{mode}{Either "Overall" or "Subgroups".}

\item{filter_expr}{Optional tidyverse-style logical expression as character.}

\item{facet_scales}{Faceting scale behaviour.}

\item{lambda_max}{Maximum lambda displayed on the x-axis.}

\item{lambda_steps}{Number of lambda evaluation points.}

\item{show_probabilities}{Numeric vector of probability levels to show as
horizontal reference lines and y-axis tick marks.}

\item{show_lambdas}{Logical; draw vertical reference lines at lambda values.}

\item{lambdas}{Numeric vector of lambda reference markers.}

\item{palette}{Manual palette vector or Brewer palette name.}

\item{return_data}{Logical; return CEAC table instead of plot.}

\item{...}{Additional parameters forwarded to geoms.}
}
\value{
A ggplot object or a CEAC data table (if return_data = TRUE).
}
\description{
Produces Cost-Effectiveness Acceptability Curves (CEACs) derived from
bootstrap replicates generated by compute_icers(). CEACs represent, for each
value of the willingness-to-pay threshold (lambda), the probability that an
alternative strategy is cost-effective relative to the reference.
}
\details{
The function follows the unified graphical architecture of plot.icers(),
adapted to the CEAC geometry (lambda–probability space). It supports
conditional reference lines for both probabilities (horizontal) and lambda
thresholds (vertical), faceting, and full aesthetic overrides.
}
