% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.marginals.R
\name{plot.marginals}
\alias{plot.marginals}
\title{Plot marginal distributions of incremental outcomes (Delta Cost or Delta Effect)}
\usage{
\method{plot}{marginals}(
  data,
  variable = "cost",
  geom_type = "histogram",
  color_by = NULL,
  shape_by = NULL,
  facet_by = NULL,
  mode = "Overall",
  filter_expr = NULL,
  facet_scales = "fixed",
  palette = NULL,
  bins = 30,
  ...
)
}
\arguments{
\item{data}{A cea_results_list object or a tibble returned by
combine_icers_results().}

\item{variable}{Character string. \code{"cost"} selects \code{Delta_Cost},
\code{"effect"} selects \code{Delta_Effect}.}

\item{geom_type}{Character string: \code{"histogram"}, \code{"density"}, or \code{"boxplot"}.}

\item{color_by}{Character variable mapped to colour, or "none".}

\item{shape_by}{Character variable mapped to shape, or "none".}

\item{facet_by}{Faceting directive ("none", "dataset", "comparison",
"subgroup_var", "subgroup_level", "subgroup").}

\item{mode}{Either "Overall" or "Subgroups".}

\item{filter_expr}{Optional tidyverse-style logical expression as character.}

\item{facet_scales}{Faceting scale behaviour.}

\item{palette}{Manual palette vector or Brewer palette name.}

\item{bins}{Integer. Number of bins when \code{geom_type = "histogram"}.}

\item{...}{Additional parameters forwarded to geoms.}
}
\value{
A \code{ggplot} object.
}
\description{
This function visualizes marginal distributions of incremental costs
(\code{Delta_Cost}) or incremental effects (\code{Delta_Effect}) using bootstrap
replicates stored in a \code{cea_results_list} object generated by
\code{compute_icers()}. Supported geometries include histogram, density,
and boxplot. The function adheres to the unified graphical conventions
of the package, including consistent mappings, palette assignment,
faceting rules, and tidy-style filtering.
}
\details{
Colour aesthetics always reflect the primary comparison grouping unless
overwritten by the user. Fill aesthetics reuse the same palette with
controlled transparency. Multiple datasets are represented using the
linetype patterns and point shapes generated internally by \code{ce_plot_base()}.

When \code{shape_by = NULL} (or \code{"none"}), all shape/linetype-related aesthetics
are fully disabled: no linetype mapping, no shape mapping, and no linetype
legend entries are used in any geometry. Geometric outlines revert to
solid lines.
}
