% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.icers.R
\name{plot.icers}
\alias{plot.icers}
\title{Plot incremental cost-effectiveness results (ICER plane)}
\usage{
\method{plot}{icers}(
  data,
  mode = "overall",
  subgroups = NULL,
  lambda = NULL,
  facet = FALSE,
  facet_by = "vars",
  alpha = 0.5,
  size = 1,
  show_means = TRUE,
  show_contours = TRUE,
  contour_level = 0.95,
  palette = "Set2"
)
}
\arguments{
\item{data}{A tibble generated by \code{\link{combine_icers_results}}.}

\item{mode}{Character. Visualization mode: "overall", "subgroups",
"bylevels", or "bygroup".}

\item{subgroups}{Optional character vector specifying which subgroup
variables to include (for modes other than "overall").}

\item{lambda}{Numeric. Willingness-to-pay threshold (optional).}

\item{facet}{Logical. If TRUE, facet the plot according to \code{facet_by}.}

\item{facet_by}{Character. For mode = "subgroups", determines the faceting
scheme: "vars" (facet by subgroup variable) or "levels" (facet by subgroup level).}

\item{alpha}{Numeric transparency for points (default = 0.5).}

\item{size}{Numeric point size (default = 1).}

\item{show_means}{Logical. If TRUE, add a cross at the mean of each group.}

\item{show_contours}{Logical. If TRUE, add a 2D density contour around each group.}

\item{contour_level}{Numeric. Probability level for the contour (default = 0.95).}

\item{palette}{Character. Color palette name from RColorBrewer (default = "Set2").}
}
\value{
A ggplot object representing the cost-effectiveness plane.
}
\description{
Produces a cost-effectiveness plane using bootstrap replicates from one or
several analyses. Supports multiple visualization modes for overall and
subgroup analyses. Optionally displays mean markers and a single 2D-density
contour (default 95\%) around each group of points.
}
